providers = ["node"]

[variables]
NODE_VERSION = "18.20.5"

[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmd = "npm ci"

[phases.build]
cmd = "npm run build"

[start]
cmd = "npm start"
